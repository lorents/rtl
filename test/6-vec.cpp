#include <rtl/vec.h>

#include "test.h"

void rtl::tests::vec_test()
{
	vec<int> foo{ 1, 3, 3, 7 };
	/*iterator it1;
	assert_insert( foo.poll(it1), 1, 0 );
	assert_insert( foo.poll(it1), 3, 1 );
	assert_insert( foo.poll(it1), 3, 2 );
	assert_insert( foo.poll(it1), 7, 3 );
	
	iterator it2;
	assert_insert( foo.poll(it2), 1, 0 );
	assert_insert( foo.poll(it2), 3, 1 );
	assert_insert( foo.poll(it2), 3, 2 );
	assert_insert( foo.poll(it2), 7, 3 );

	foo.insert(2, 1337);
	assert_insert( foo.poll(it1), 1337, 2 );*/
}
